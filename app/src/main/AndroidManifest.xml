<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADVERTISE" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logo_splash"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo_splash_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".SymptomsHistory"
            android:exported="false"
            android:label="@string/title_activity_symptomps_history"
            android:theme="@style/Theme.MyApplication.NoActionBar" />
        <activity
            android:name=".MeasurePage"
            android:exported="false" />
        <activity
            android:name=".SymptompsPage"
            android:exported="false" />
        <activity
            android:name=".ManualMeasure"
            android:exported="false" />
        <activity
            android:name=".AutomaticMeasure"
            android:exported="false" />
        <activity android:name=".SignUpActivity" />

        <meta-data
            android:name="com.google.firebase.firestore.auth_token_enabled"
            android:value="false" />

        <activity android:name=".LoginActivity" />
        <activity android:name=".HomePage" />
        <activity
            android:name=".History"
            android:value="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MyApplication"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>